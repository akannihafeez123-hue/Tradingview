{
  "name": "signal-engine",
  "version": "9.0.0",
  "description": "Kleez Institutional Intelligence System — Full-Universe Crypto Signal Engine with Keras Neural Brain",
  "main": "Render_workspace.js",
  "private": true,

  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },

  "scripts": {
    "start": "node Render_workspace.js",
    "dev": "node --watch Render_workspace.js",
    "health": "curl -s http://localhost:10000/status | node -e \"process.stdin.resume(); let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>console.log(JSON.parse(d).status || 'OK'))\""
  },

  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.8",
    "ws": "^8.16.0",
    "dotenv": "^16.4.5",
    "@tensorflow/tfjs-node": "^4.17.0"
  },

  "optionalDependencies": {
    "@tensorflow/tfjs": "^4.17.0"
  },

  "keywords": [
    "trading",
    "crypto",
    "signals",
    "institutional",
    "bitget",
    "telegram",
    "neural-network",
    "lstm",
    "apex",
    "ghost-engine"
  ],

  "author": "Kleez",
  "license": "UNLICENSED"
}
